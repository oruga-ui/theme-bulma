import copy from "rollup-plugin-copy"
import scss from "rollup-plugin-scss"

const entries = {
  index: "src/plugins/oruga.ts",
  scss: "src/assets/scss/plugin.scss",
}

export default function() {
  const config = []
  return config
}
